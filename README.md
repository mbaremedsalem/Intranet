# Intranet
# to install project
# 1 - create vuritual invirement python -m venv.venv
# 2 - activate viritual invirement 
# a - for windows :
#      - Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
#      - .venv\Scripts\Activate.ps1
# b - for macOs:
#      - source .venv/bin/activate
# and then you will install all package used on this project with command 
#      - pip install - r requirement.txt   
